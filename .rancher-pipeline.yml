stages:
  - name: Build
    steps:
      - runScriptConfig:
          image: maven:3-adoptopenjdk-11
          shellScript: git submodule init && git submodule update && mvn package -DskipTests
  - name: Build Docker with DIND
    steps:
      - publishImageConfig:
          dockerfilePath: ./Dockerfile
          buildContext: .
          tag: docker.dev.folio.org/mod-feesfines:modfee-98-rancher2
          pushRemote: true
          registry: docker.dev.folio.org
  - name: Deploy
    steps:
      - applyAppConfig:
          catalogTemplate: p-jqjbc:vega-helmcharts-mod-feesfines
          version: 0.1.32
          answers:
            image.repository: docker.dev.folio.org/mod-feesfines
            image.tag: modfee-98-rancher2
          targetNamespace: vega
          name: mod-feesfines
timeout: 60
notification: {}
